#
# You can create an unlimited number of placeholders
# with a specified range. For example, 'exampleRandom' will be replaced
# with a random number between 1 and 3 (1, 2 or 3).
#
# In reward's .yml you can use it like:
#  commands:
#     - crate key give %player% premiumCrate %exampleRandom%
#
placeholders:
  exampleRandom: 1-3
  anotherRandom: 10-10000
